<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="trustfulIP.mapper.MessagecropuserMapper">
    <select id="selectByStoreId" resultType="trustfulIP.Dto.entity.Messagecropuser" parameterType="Integer">
        SELECT * FROM messagecropuser
        WHERE message_store_id = #{storeId}
    </select>

</mapper>
